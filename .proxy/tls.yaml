tls:
  stores:
    default:
      defaultCertificate:
        certFile: /etc/certs/frontend/cert.pem
        keyFile: /etc/certs/frontend/key.pem
  certificates:
    - certFile: /etc/certs/frontend/cert.pem
      keyFile: /etc/certs/frontend/key.pem
